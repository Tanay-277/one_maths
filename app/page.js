export default function Home() {
  return (
    <div>
      <h1>Hello, world!</h1>
      <p>
        This is a page in a Next.js app. It's a React component, but it's also
        a page because it's in the <code>pages</code> directory.
      </p>
    </div>
  );
}
